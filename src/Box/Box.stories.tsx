import React from "react";
import Box  from '../Box/Box';

const Boxstory = (props) =>{
    return(<Box onClick={() => { }}/>)
}

export default { title: "Box" };
export const WithBox = () => <Boxstory />;