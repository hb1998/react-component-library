import React from "react";
import Icon  from '../Icon/Icon';

const Iconstory = (props) =>{
    return(<Icon onClick={() => { }}/>)
}

export default { title: "Icon" };
export const WithIcon = () => <Iconstory />;